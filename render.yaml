version: "1"

services:
  - type: web
    name: shachina
    runtime: docker
    dockerfilePath: Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: PORT
        value: 8000
      - key: ENVIRONMENT
        value: production
      - key: PROJECT_TITLE
        value: "SHACHINA Quantitative Trading Platform"
      - key: OWNER_NAME
        value: "Bibek"
      - key: PRIMARY_MARKET
        value: "NEPSE"
      - key: DEFAULT_TIMEZONE
        value: "Asia/Kathmandu"
      - key: DEFAULT_CURRENCY
        value: "NPR"
      - key: SHACHINA_SECRET_KEY
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: "*"
      - key: DATABASE_URL
        fromDatabase:
          name: shachina-db
          property: connectionString

databases:
  - name: shachina-db
    databaseName: shachina
    user: shachina_user
    plan: free
    region: singapore
